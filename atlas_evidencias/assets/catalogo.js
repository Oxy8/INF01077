const search=document.querySelector('#search');const status=document.querySelector('#status-filter');const topic=document.querySelector('#topic-filter');const rows=[...document.querySelectorAll('.catalog-row')];const count=document.querySelector('#visible-count');function update(){const q=(search.value||'').toLocaleLowerCase('pt-BR');let shown=0;for(const row of rows){const ok=(!q||row.dataset.search.includes(q))&&(!status.value||row.dataset.status===status.value)&&(!topic.value||row.dataset.topic===topic.value);row.hidden=!ok;if(ok)shown++}count.textContent=`${shown} de ${rows.length} visualizações`};for(const control of [search,status,topic])control.addEventListener('input',update);update();
